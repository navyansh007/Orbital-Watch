{
  "name": "orbital-watch",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "predev": "node scripts/copy-cesium-assets.mjs",
    "dev": "vite",
    "prebuild": "node scripts/copy-cesium-assets.mjs",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "preview:cf": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "lint": "oxlint",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "cesium": "^1.145.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "satellite.js": "^7.1.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.7",
    "@vitejs/plugin-react": "^6.1.1",
    "oxlint": "^1.81.0",
    "typescript": "~6.0.2",
    "vite": "^8.3.0",
    "wrangler": "^4.137.0"
  },
  "description": "Live real-physics satellite tracker with coverage footprints and space-weather risk."
}
