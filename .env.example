# Free Cesium Ion access token — https://cesium.com/ion/tokens
# Required for the default Earth imagery and terrain. Client-side by nature,
# so it must carry Vite's VITE_ prefix to be exposed to the bundle.
VITE_CESIUM_ION_TOKEN=
